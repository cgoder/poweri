# 05 — 并发控制（会话内串行 / 跨会话并行）

**What to build:** 网关对同一用户同一会话只允许一个 in-flight 请求（其余排队），不同会话可并行执行，防止并发写坏同一会话 JSONL。各会话是独立文件，跨会话并行天然安全。

**Blocked by:** 04 — 会话续接 + 每用户 PVC 挂载

**Status:** ready-for-agent

- [ ] 同一会话两个并发请求被串行化（后一个等前一个完成）
- [ ] 不同会话的两个请求可并行执行
- [ ] 并发下会话 JSONL 不损坏、不丢数据
